export default class UsuarioClass {
    constructor(
        nombreUsuario,
        contrasenia,
        rol,
    ) {
        this.nombreUsuario = nombreUsuario;
        this.contrasenia = contrasenia;
        this.rol = rol;
    }
}